- name: Setup Frontend
  hosts: all
  become: yes
  tasks:
    - name: Installing nginx Server
      ansible.builtin.yum:
        name: nginx
        state: latest

    - name: remove directory
      Ansible.builtin.file:
          Path: /usr/share/nginx/html
          State: absent



